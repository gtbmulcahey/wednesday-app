import { Link } from "react-router-dom"

export const Footer = () => {
    return (
      <footer>
        <div>
          <nav>
            <Link to="/about">About</Link>
          </nav>
        </div>
      </footer>
    )
  }
  